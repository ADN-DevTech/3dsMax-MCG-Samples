(InstanceState state, Boolean Weld, Single threshold, TriMesh mesh) => 
  {
    if (Weld)
      {
        TriMesh _0WeldMesh;// 0:WeldMesh 91693
        _0WeldMesh = WeldMesh(threshold, mesh);
        _0WeldMesh;
        }else
      {
        mesh;
        };
    }